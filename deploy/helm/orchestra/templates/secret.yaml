# Placeholder — actual secret values are injected by CI/CD or external-secrets operator.
# Never commit real credentials. Use sealed-secrets, Vault, or AWS/GCP Secret Manager.
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "orchestra.fullname" . }}-secrets
  labels: {{- include "orchestra.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
data: {}
